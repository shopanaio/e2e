query ProductContainerCategory($handle: String!) {
  product(handle: $handle) {
    id
    handle
    product {
      id
      handle
      category {
        id
        handle
        title
      }
    }
  }
}
